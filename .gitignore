# Dependencies
node_modules/
package-lock.json
yarn.lock

# Build outputs
dist/
build/
out/

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# System Files
.DS_Store
Thumbs.db
*.log

# Environment files
.env
.env.local
.env.*.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.swp
*.swo
*~

# Optional: Cache directories
.cache/
.temp/
```

This `.gitignore` file:
1. Excludes common dependency directories and lock files
2. Excludes build output directories
3. Selectively includes VS Code settings while ignoring other VS Code files
4. Ignores system-generated files
5. Protects sensitive environment files
6. Ignores debug logs
7. Excludes temporary files and cache directories

Note that I've kept `.vscode/settings.json` tracked since it contains your live server port configuration which might be important for team consistency.